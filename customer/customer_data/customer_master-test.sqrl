-- ## Customer Master Data - Test Data
--
-- Use local filesystem data for testing

IMPORT customer_master.*;

CREATE TABLE _Customer_input (
  /** Timestamp when record was ingested into the data platform */
  ingested_at TIMESTAMP_LTZ(3) NOT NULL,
  WATERMARK FOR ingested_at AS ingested_at - INTERVAL '10' SECOND
) WITH (
    'connector' = 'filesystem',
    'path' = '${DATA_PATH}/customer_master-customer.jsonl',
{{^is_batch}}
    'source.monitor-interval' = '10 min',
{{/is_batch}}
    'format' = 'json',
    'json.timestamp-format.standard' = 'ISO-8601'
) LIKE Customer_schema;

Customer := SELECT * FROM _Customer_input WHERE customer_id <> '';

CREATE TABLE _Customer_Address_input (
    /** Timestamp when record was ingested into the data platform */
    ingested_at TIMESTAMP_LTZ(3) NOT NULL,
    WATERMARK FOR ingested_at AS ingested_at - INTERVAL '10' SECOND
) WITH (
    'connector' = 'filesystem',
    'path' = '${DATA_PATH}/customer_master-customer_address.jsonl',
{{^is_batch}}
    'source.monitor-interval' = '10 min',
{{/is_batch}}
    'format' = 'json',
    'json.timestamp-format.standard' = 'ISO-8601'
) LIKE Customer_Address_schema;

Customer_Address := SELECT * FROM _Customer_Address_input WHERE customer_id <> '';

CREATE TABLE _Customer_Contact_input (
  /** Timestamp when record was ingested into the data platform */
  ingested_at TIMESTAMP_LTZ(3) NOT NULL,
  WATERMARK FOR ingested_at AS ingested_at - INTERVAL '10' SECOND
) WITH (
    'connector' = 'filesystem',
    'path' = '${DATA_PATH}/customer_master-customer_contact.jsonl',
{{^is_batch}}
    'source.monitor-interval' = '10 min',
{{/is_batch}}
    'format' = 'json',
    'json.timestamp-format.standard' = 'ISO-8601'
) LIKE Customer_Contact_schema;

Customer_Contact := SELECT * FROM _Customer_Contact_input WHERE customer_id <> '';

CREATE TABLE _Customer_Employment_input (
  /** Timestamp when record was ingested into the data platform */
  ingested_at TIMESTAMP_LTZ(3) NOT NULL,
  WATERMARK FOR ingested_at AS ingested_at - INTERVAL '10' SECOND
) WITH (
    'connector' = 'filesystem',
    'path' = '${DATA_PATH}/customer_master-customer_employment.jsonl',
{{^is_batch}}
    'source.monitor-interval' = '10 min',
{{/is_batch}}
    'format' = 'json',
    'json.timestamp-format.standard' = 'ISO-8601'
) LIKE Customer_Employment_schema;

Customer_Employment := SELECT * FROM _Customer_Employment_input WHERE customer_id <> '';

CREATE TABLE _Customer_Relationship_input (
  /** Timestamp when record was ingested into the data platform */
  ingested_at TIMESTAMP_LTZ(3) NOT NULL,
  WATERMARK FOR ingested_at AS ingested_at - INTERVAL '10' SECOND
) WITH (
    'connector' = 'filesystem',
    'path' = '${DATA_PATH}/customer_master-customer_relationship.jsonl',
{{^is_batch}}
    'source.monitor-interval' = '10 min',
{{/is_batch}}
    'format' = 'json',
    'json.timestamp-format.standard' = 'ISO-8601'
) LIKE Customer_Relationship_schema;

Customer_Relationship := SELECT * FROM _Customer_Relationship_input WHERE primary_customer_id <> '';
